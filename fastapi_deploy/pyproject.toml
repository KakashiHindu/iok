[project]
name = "signbridge-fastapi-deploy"
version = "0.1.0"
dependencies = ["fastapi", "uvicorn[standard]"]
requires-python = ">=3.11"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]
